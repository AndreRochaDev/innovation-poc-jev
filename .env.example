# Copiar para .env e preencher (este ficheiro É seguro commitar — nunca ponhas chaves reais aqui)
#   Copy-Item .env.example .env   (Windows)  |  cp .env.example .env   (Linux/Mac)
OPENCODE_API_KEY=cola-aqui-a-tua-chave-zen
JEV_MODEL=jev-1.13-free
VITE_API_URL=http://localhost:5000
